# 石斛病虫害识别项目 Flutter 开发任务拆分文档

## 1. 文档目标

本文档用于把石斛病虫害识别项目 Flutter 前端工作拆分为可执行任务，并标记这些任务在当前仓库中的真实状态，便于后续继续开发、审查和提交流程使用。

## 2. 状态说明

任务状态统一使用以下三类：

- `已完成`：代码已经落地，且当前基线可用
- `部分完成`：骨架或部分能力已完成，但未达到原任务的完整验收标准
- `待完成`：当前尚未真正实现，只是保留了入口、契约或规划

## 3. 里程碑状态总览

- `M0` 项目初始化与工程底座：已完成
- `M1` 网络层、模型层、配置层：已完成
- `M2` 公共组件与主题：已完成
- `M3` 启动、登录、首页、单图识别、结果页：大部分完成，真实接口联调仍待收口
- `M4` 实时识别、历史记录、设置：大部分完成，实时识别仍待实现
- `M5` 测试、稳定性、发布准备：部分完成

## 4. 任务清单

### M0 项目初始化与工程底座

#### T001 创建 Flutter 项目基础结构

状态：`已完成`

当前交付：

- `lib/`
- `doc/`
- `test/`
- Android / iOS / macOS / Web / Windows / Linux / OpenHarmony 平台工程目录

说明：

- `integration_test/` 当前尚未创建，因此原任务中的该项交付需要放到后续集成测试任务中补齐。

#### T002 配置主题与路由骨架

状态：`已完成`

当前交付：

- `app.dart`
- `theme.dart`
- `routes.dart`

#### T003 配置代码规范与分析工具

状态：`已完成`

当前交付：

- `analysis_options.yaml`
- 格式化与分析命令约定

### M1 网络层、模型层、配置层

#### T004 实现环境配置层

状态：`已完成`

当前交付：

- `env_config.dart`
- `development / staging / production` 三类环境配置

#### T005 实现 `ApiClient`

状态：`已完成`

当前交付：

- `dio` 基础封装
- 超时、请求头、错误处理
- `GET / POST / multipart` 基础方法

#### T006 实现接口响应模型

状态：`已完成`

当前交付：

- `ApiException`
- 网络异常映射
- 通用 `ApiResponse<T>` 类型封装
- 业务错误码到强类型错误的统一映射

#### T007 实现识别相关模型

状态：`已完成`

当前交付：

- `DetectResponse`
- `DetectionItem`
- `BoundingBox`
- `AdviceInfo`
- `DetectSummary`
- `ImageInfo`
- `ModelInfo`
- 核心识别模型已统一接入 `json_serializable`

#### T008 实现历史记录模型与本地配置模型

状态：`已完成`

当前交付：

- `HistoryItem`
- `HistoryRecord`
- `AppSettings`
- 本地配置和历史记录模型已统一接入 `json_serializable`

### M2 公共组件与主题

#### T009 实现基础设计令牌

状态：`已完成`

当前交付：

- 统一主题色
- 页面圆角与阴影
- 统一页面视觉基线

#### T010 实现通用组件

状态：`已完成`

当前交付：

- `CommonButton`
- `CommonCard`
- `LoadingView`
- `EmptyView`
- `ResultInfoCard`
- 自适应图片展示组件

### M3 首页、单图识别、结果页

#### T011 实现启动页

状态：`已完成`

当前交付：

- Logo
- 初始化流程
- 错误重试
- 跳转逻辑

说明：

- 当前已把本地登录态恢复纳入启动初始化流程。

#### T011A 实现登录页与路由守卫

状态：`已完成`

当前交付：

- `LoginPage`
- `AuthController`
- `AuthRepository` 抽象
- 支持平台的安全登录会话持久化
- `OpenHarmony / 鸿蒙` 进程内会话缓存兜底
- 受保护路由跳转控制

尚缺：

- 真实登录接口联调

#### T012 实现首页

状态：`已完成`

当前交付：

- 项目标题区
- 功能入口区
- 基线说明区

#### T013 实现图片选择与预览能力

状态：`已完成`

当前交付：

- 相册选择
- 拍照入口
- 图片预览
- 文件失败提示

说明：

- 桌面端拖拽仍是后续项，不计入当前任务完成标准。

#### T014 实现单图识别状态管理

状态：`已完成`

当前交付：

- `DetectController`
- `idle / running / success / failed` 状态流

#### T015 接入单图识别接口

状态：`部分完成`

当前交付：

- `DetectRepository` 抽象
- `MockDetectRepository` 受控替身实现
- `RealDetectRepository` 真实 `/api/v1/detect/image` 实现
- `DetectRepository Provider` 已支持开发 / 测试环境切换 mock
- 已对超时、限流、服务暂不可用等瞬时失败接入有限次自动重试
- 结果页和历史记录已按标准结果模型打通

尚缺：

- 与后端契约的正式联调
- 更完整的错误码细化与联调验证

#### T016 实现结果页

状态：`已完成`

当前交付：

- 摘要信息
- 检测列表
- 严重程度
- 防治建议
- 空结果展示

#### T017 实现结果保存为历史记录

状态：`已完成`

当前交付：

- 本地保存逻辑
- 成功反馈
- 从历史记录回看结果详情

### M4 实时识别、历史记录、设置

#### T018 实现实时识别页画面结构

状态：`已完成`

当前现状：

- 已有路由入口
- 已接入预览区、摘要区、检测框元数据区和会话控制区
- 已明确展示“未接入真实摄像头链路”的边界说明
- 已支持测试帧链路下的摘要与检测框元数据展示

#### T019 实现实时识别状态管理

状态：`已完成`

当前交付：

- 会话状态
- 运行 / 暂停控制
- Overlay 数据刷新

#### T020 接入实时识别接口或测试数据链路

状态：`部分完成`

当前交付：

- 实时识别测试链路

尚缺：

- 摄像头取帧后的 `/detect/realtime/frame` 页面级接入

#### T021 实现历史记录页

状态：`已完成`

当前交付：

- 列表展示
- 空状态
- 删除单条
- 清空全部
- 历史记录 Repository 抽象

#### T022 实现设置页与关于页

状态：`已完成`

当前交付：

- 版本信息
- 平台信息
- 登录会话信息
- `baseUrl` 配置
- 服务健康检查
- 本地数据管理
- 退出登录
- 关于项目页

#### T022A 预留远程历史记录数据源，可选

状态：`待完成`

当前现状：

- 页面层与本地仓储已解耦
- 远程历史记录实现尚未补齐

### M5 测试、稳定性、发布准备

#### T023 补齐单元测试

状态：`部分完成`

当前交付：

- `auth_storage` 相关测试
- `auth_controller` 相关测试
- `real_auth_repository` 相关测试
- `detect_controller` 相关测试
- `history_record` 相关测试
- `history_repository` 相关测试
- `ApiClient` 网络错误处理测试

尚缺：

- 更多模型解析测试

#### T024 补齐 Widget 测试

状态：`已完成`

当前交付：

- 路由守卫决策测试
- `common_button` 测试
- `detect_page` 测试
- `home_page` 测试
- `history_page` 测试
- `settings_page` 测试
- `result_page` 测试

#### T025 补齐关键集成测试

状态：`待完成`

当前现状：

- `integration_test/` 尚未建立

#### T026 接入日志与异常采集

状态：`待完成`

尚缺：

- `AppLogger`
- 异常采集方案

#### T027 发布前回归与构建验证

状态：`部分完成`

当前交付：

- `flutter analyze`
- `flutter test`
- `flutter build linux --debug`

尚缺：

- Android、iOS、Web、Windows、macOS、OpenHarmony 的构建验证
- 发布检查清单
- 版本与构建号回归确认

## 5. 推荐执行顺序

后续建议按以下顺序继续推进：

1. `T006`
2. `T015`
3. `T018 ~ T020`
4. `T023 ~ T027`

说明：

- 不要在真实单图识别接口未接入前先扩大量级更高的实时识别逻辑
- 不要把“占位页”继续包装成“已实现页”
- 每次任务状态变化后必须同步更新本文档

## 6. 单任务完成定义

每个任务都必须满足以下标准才算真正完成：

- 代码已实现
- 自测通过
- 相关文档已同步
- 公共注释已同步
- 不引入静默降级逻辑
- 满足对应测试或验收要求

## 7. 结论

本文档现在用于标记“当前仓库离完整交付还差哪些步骤”，而不是继续停留在早期规划列表。后续开发、审查和提交前检查，都应以这里的真实任务状态为准。
