# 斛生 前后端接口契约文档

## 1. 文档目标

本文档不再以旧版规划稿为准，而是以当前工作区内实际可见的后端源码为准，说明 Flutter 前端与后端的真实接口契约、会话方式和当前联调边界。

当前已确认的本地后端工程：

- 同级目录 `../shihu-web`
- 技术栈：Spring Boot
- 真实控制器：`LoginController`、`DeviceApiController`

## 2. 当前已确认的后端接口

当前 `origin/web` 分支里可以确认的后端接口共 7 个，其中 Flutter 当前已接入其中 6 个：

- `POST /api/login`
- `POST /api/register`
- `GET /api/check-login`
- `POST /api/logout`
- `GET /api/status`
- `POST /api/ops/led`
- `GET /api/health`

此外，Flutter 当前还按 `doc/java-video-collaboration.md` 接入了 Java 视频协作接口：

- `GET /api/video/streams`
- `GET /api/video/streams/{streamId}`

说明：

- 上述接口均来自 `../shihu-web` 的真实控制器源码。
- 当前工作区里没有发现 `/api/v1/auth/*`、`/api/v1/detect/image`、`/api/v1/detect/realtime/frame` 对应的后端实现。
- 因此前端文档和页面中凡是把这些 `/api/v1/*` 路径描述成“本地已联调完成”的内容，均视为过时描述。
- 当前 Flutter 仓库默认 `BASE_URL` 已改为部署地址 `http://101.35.79.76:8082`，便于直接联调线上设备服务。
- 视频协作接口默认与设备服务同主机部署，但固定使用 `19081` 端口。
- Flutter 侧通过当前 `baseUrl` 自动派生视频服务地址，不要求页面层手写端口拼接。

## 3. 认证契约

### 3.1 会话模式

当前后端认证不是 Bearer Token 模式，而是：

- `HttpSession`
- `JSESSIONID` Cookie

前端约束：

- Web 端仅在 `/api/login / /api/check-login / /api/logout` 这类认证链路上启用 `withCredentials`，交由浏览器自动管理 Cookie
- `/api/status / /api/ops/led / /api/health` 当前按普通跨域请求访问，不额外声明浏览器凭据，避免被 `DeviceApiController` 的 `Access-Control-Allow-Origin: *` 拦截
- 非 Web 端需要保存并回传会话 Cookie
- 不再把旧文档中的 `refreshToken`、`Authorization: Bearer <token>` 当成当前后端真实要求

### 3.2 `POST /api/login`

用途：

- 账号密码登录
- 登录成功后由后端在响应头写入会话 Cookie

请求体：

```json
{
  "username": "root",
  "password": "12345"
}
```

说明：

- 当前 `origin/web` 分支 `application.yml` 默认登录配置为 `root / 12345`
- 实际部署环境仍以 `app.login.username`、`app.login.password` 外部配置为准

响应体：

```json
{
  "success": true,
  "message": "登录成功"
}
```

失败响应示例：

```json
{
  "success": false,
  "message": "用户名或密码错误"
}
```

字段说明：

- `success`：`bool`，是否登录成功
- `message`：`String`，登录结果说明

### 3.3 `POST /api/register`

用途：

- 创建新账号
- 当前后端注册成功后不会自动登录，前端应提示用户回到登录模式继续认证

请求体：

```json
{
  "username": "new_user",
  "password": "demo123456",
  "confirmPassword": "demo123456"
}
```

成功响应示例：

```json
{
  "success": true,
  "message": "注册成功，请使用新账号登录"
}
```

失败响应示例：

```json
{
  "success": false,
  "message": "用户名已存在，请更换用户名"
}
```

字段说明：

- `username`：`String`，3 到 32 位，只允许字母、数字和下划线
- `password`：`String`，6 到 32 位
- `confirmPassword`：`String`，需与 `password` 一致

### 3.4 `GET /api/check-login`

用途：

- 检查当前会话是否仍然有效
- 启动恢复登录态时由前端主动调用

成功响应示例：

```json
{
  "loggedIn": true,
  "username": "demo"
}
```

未登录响应示例：

```json
{
  "loggedIn": false
}
```

字段说明：

- `loggedIn`：`bool`，当前会话是否有效
- `username`：`String?`，登录成功时返回

### 3.5 `POST /api/logout`

用途：

- 注销当前会话

请求体：

- 无强制字段

响应体：

```json
{
  "success": true,
  "message": "已登出"
}
```

## 4. 设备状态接口

### 4.1 `GET /api/status`

用途：

- 拉取当前设备缓存状态
- 用于设备监测、设置页状态卡片和后续实时监控页面

响应结构：

```json
{
  "deviceId": "dev_001",
  "deviceName": "石斛培育柜",
  "temperature": { "value": 24.5, "unit": "°C" },
  "humidity": { "value": 82.0, "unit": "%" },
  "light": { "value": 1500.0, "unit": "Lux" },
  "mq2": { "value": 18.0, "unit": "ppm" },
  "error": { "value": 0 },
  "led": { "value": true },
  "updatedAt": 1741399200000
}
```

字段说明：

- `deviceId`：`String`
- `deviceName`：`String`
- `temperature / humidity / light / mq2`：对象结构，包含 `value` 与 `unit`
- `error.value`：`int`，当前错误码
- `led.value`：`bool`，LED 当前状态
- `updatedAt`：`long`，毫秒时间戳

错误码语义：

- `0`：系统运行正常
- `1`：系统预警，建议人工复核
- `2`：系统严重告警
- 其他值：前端按未知状态展示

## 5. LED 控制接口

### 5.1 `POST /api/ops/led`

用途：

- 控制设备 LED 开关

请求体：

```json
{
  "deviceId": "dev_001",
  "deviceName": "石斛培育柜",
  "led": true
}
```

响应体：

```json
{
  "status": "accepted",
  "requestId": "req_001",
  "message": "指令已下发"
}
```

排队响应示例：

```json
{
  "status": "pending",
  "requestId": "req_002",
  "message": "OneNET API调用失败,已登记到待处理队列: ..."
}
```

说明：

- 控制器返回 HTTP `202 Accepted`
- `deviceId` 在后端 DTO 上带 `@NotBlank` 约束，不能为空
- 后端当前会返回 `accepted` 或 `pending`
- 前端当前把 `accepted / success / ok / pending` 都视为后端已接收，并展示后端返回的 `message` 与 `requestId`
- 当前前端仅在设备状态已返回非空 `deviceId` 时开放 LED 开关，避免把后端参数校验错误暴露为常规交互

## 6. 视频流接口

### 6.1 `GET /api/video/streams`

用途：

- 拉取当前全部视频流的元数据
- 为视频中心页提供流清单、播放地址和 AI 转发状态

部署地址：

- 基础地址：`http://101.35.79.76:19081`
- 完整路径：`GET /api/video/streams`

成功响应示例：

```json
{
  "code": 0,
  "msg": "ok",
  "data": [
    {
      "streamId": "k230",
      "deviceId": "k230",
      "displayName": "K230 实时视频流",
      "gatewayPageUrl": "http://101.35.79.76:1984/",
      "playerUrl": "http://101.35.79.76:1984/stream.html?src=k230&mode=webrtc,mse",
      "preferredMode": "webrtc",
      "fallbackMode": "mse",
      "publicHost": "101.35.79.76",
      "webrtcPort": 8555,
      "available": true,
      "aiResultForwarded": false
    }
  ]
}
```

字段说明：

- `code`：`int`，当前约定 `0` 或 `200` 表示成功
- `msg`：`String`，业务提示
- `data`：`List<VideoStreamInfo>`，视频流列表

前端约束：

- 客户端只消费元数据，不代理视频字节流
- 当服务未返回合法 JSON 时，视频中心必须展示失败态，不允许使用假流数据

### 6.2 `GET /api/video/streams/{streamId}`

用途：

- 拉取单路视频流详情
- 为流详情页展示播放地址、网关地址和 AI 转发状态

路径参数：

- `streamId`：`String`，当前至少支持 `k230`

成功响应示例：

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "streamId": "k230",
    "deviceId": "k230",
    "displayName": "K230 实时视频流",
    "gatewayPageUrl": "http://101.35.79.76:1984/",
    "playerUrl": "http://101.35.79.76:1984/stream.html?src=k230&mode=webrtc,mse",
    "preferredMode": "webrtc",
    "fallbackMode": "mse",
    "publicHost": "101.35.79.76",
    "webrtcPort": 8555,
    "available": true,
    "aiResultForwarded": false
  }
}
```

## 7. AI 结果协作边界

当前前端范围仍然保持不变：

- 不直接从 RK3568 拉取 `/api/ai` 或 `latest_ai_result`
- 不直接消费 `AI_DETECTIONS` 原始 JSON
- 当前客户端只展示视频服务返回的 `aiResultForwarded` 摘要字段

如果后续需要在客户端展示 AI 结果，必须由 Java 服务先实现自己的查询接口或推送接口，再补充新的前端契约。

## 8. 健康检查接口

### 8.1 `GET /api/health`

用途：

- 快速检查后端服务是否存活

当前后端返回：

```text
ok
```

前端处理约定：

- 返回字符串 `ok` 视为服务正常
- 当前前端会将其映射为 `ServiceHealthInfo(status: "up", serviceName: "设备运行服务", serviceVersion: "标准部署", modelStatus: "ready")`

## 9. 错误处理约定

当前本地后端并没有统一的 `ApiResponse<T>` 包裹结构，至少以下接口不是：

- `/api/login`
- `/api/register`
- `/api/check-login`
- `/api/logout`
- `/api/status`
- `/api/ops/led`
- `/api/health`

补充说明：

- 登录校验失败返回 `{ success: false, message }`
- 参数校验异常由 `GlobalExceptionHandler` 返回 `400`
- LED 控制异常通常返回 `{ status: "error", requestId: null, message }`
- 前端网络层会优先透传后端响应体中的 `message`，不再只显示通用状态码文案
- 因此前端必须按接口类型分别解析，不能再强行假设全部接口统一包裹 `code / message / data`

## 10. 前端当前实现约束

当前 Flutter 前端已经按以下原则收口：

- `AuthRepository` 真实实现对接 `/api/login`、`/api/register`、`/api/check-login`、`/api/logout`
- `ApiClient` 在 Web 端按链路选择是否启用 `withCredentials`
- `ApiClient` 在非 Web 端支持附带持久化的会话 Cookie
- `ServiceHealthRepository` 只对接 `/api/health`
- `DeviceStateRepository` 对接 `/api/status` 与 `/api/ops/led`
- `VideoStreamRepository` 对接 `/api/video/streams` 与 `/api/video/streams/{streamId}`

## 11. 当前未确认的识别接口

以下接口在 Flutter 前端代码里仍有仓储或页面预留，但当前工作区没有发现对应后端源码：

- `/api/v1/detect/image`
- `/api/v1/detect/realtime/frame`

因此当前结论必须写清楚：

- 前端仍保留识别相关模型、页面和仓储扩展点
- 但不能把这些接口写成“当前本地后端已经完成联调”
- 如果后续要继续对接，必须先补充独立识别服务的真实仓库位置或源码

## 10. 文档维护要求

后续如果出现以下任一变化，必须同步修改本文档：

- 认证模式从 Session 切到 Token
- 登录或设备接口路径变化
- `/api/status` DTO 字段变化
- LED 控制结果结构变化
- 识别服务后端源码并入工作区
