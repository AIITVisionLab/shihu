import 'package:flutter/material.dart';

/// 应用使用的主配色与辅助配色。
class AppPalette {
  /// 更深一层的松绿色，用于标题和强调区。
  static const Color deepPine = Color(0xFF2F4739);

  /// 深色文字与图标主色。
  static const Color pineInk = Color(0xFF212D24);

  /// 参考图主色，作为整体识别色。
  static const Color pineGreen = Color(0xFF518463);

  /// 辅助松绿色，用于温和强调。
  static const Color softPine = Color(0xFFA3C6AE);

  /// 偏雾感的浅青绿色，用于信息卡片和轻背景。
  static const Color mistMint = Color(0xFFD7E3D4);

  /// 带一点亚麻感的浅米绿色，用于中性强调。
  static const Color linenOlive = Color(0xFFD8D3B0);

  /// 参考图中的淡紫灰，用于辅助分层。
  static const Color softLavender = Color(0xFFD5CBD6);

  /// 纸感底色。
  static const Color paper = Color(0xFFEEE4D3);

  /// 更明亮一层的纸感底色。
  static const Color paperWarm = Color(0xFFF6F0E3);

  /// 稍深一层的纸感底色。
  static const Color paperShade = Color(0xFFE2D7C4);

  /// 略带暖灰的宣纸底色，用于大面积衬底。
  static const Color paperMist = Color(0xFFF2EBDD);

  /// 更亮一层的宣纸高光底色。
  static const Color paperSnow = Color(0xFFFBF6EB);

  /// 浅雾松青，用于弱化背景和高亮边框。
  static const Color fogMint = Color(0xFFEAE3D6);

  /// 统一描边色。
  static const Color outlineSoft = Color(0xFFD8CDB9);
}
