import 'package:flutter/material.dart';

/// 应用使用的主配色与辅助配色。
class AppPalette {
  /// 更深一层的松绿色，用于标题和强调区。
  static const Color deepPine = Color(0xFF2F4739);

  /// 深色文字与图标主色。
  static const Color pineInk = Color(0xFF212D24);

  /// 参考图主色，作为整体识别色。
  static const Color pineGreen = Color(0xFF518463);

  /// 辅助松绿色，用于温和强调。
  static const Color softPine = Color(0xFFA7D3B2);

  /// 偏雾感的浅青绿色，用于信息卡片和轻背景。
  static const Color mistMint = Color(0xFFC9E2D0);

  /// 带一点亚麻感的浅米绿色，用于中性强调。
  static const Color linenOlive = Color(0xFFD2D6AC);

  /// 参考图中的淡紫灰，用于辅助分层。
  static const Color softLavender = Color(0xFFCEBBD8);

  /// 纸感底色。
  static const Color paper = Color(0xFFEFEFEA);

  /// 更明亮一层的纸感底色。
  static const Color paperWarm = Color(0xFFF7F7F3);

  /// 稍深一层的纸感底色。
  static const Color paperShade = Color(0xFFE1E3DC);

  /// 冷一点的雾面纸色，用于大面积衬底。
  static const Color paperMist = Color(0xFFF1F2ED);

  /// 更亮一层的冷白底色。
  static const Color paperSnow = Color(0xFFFAFAF6);

  /// 浅雾松青，用于弱化背景和高亮边框。
  static const Color fogMint = Color(0xFFE8ECE5);

  /// 统一描边色。
  static const Color outlineSoft = Color(0xFFD6DBD3);
}
